// import { neja } from "neja"

// const { config } = neja.parseArgs({
// 	config: { type: "enum", values: ["dev", "npm"] },
// } as const)

// neja.setFlags<typeof import("./rules.neja.ts").flags>({
// 	config,
// })
